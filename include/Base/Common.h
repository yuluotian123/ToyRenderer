#pragma once
#include <string>
constexpr  static int SCREEN_WIDTH = 1980;
constexpr static  int SCREEN_HEIGHT = 1080;
constexpr static float SCREEN_ASPECT_RATIO = SCREEN_WIDTH / (float)SCREEN_HEIGHT;

typedef int Shaderid;
typedef std::string Materialid;